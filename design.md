# 인연사주 디자인 시스템 (design.md)

> 이 문서는 인연사주의 모든 UI 작업에 적용되는 단일 기준이다.
> 새 화면·컴포넌트·문구를 만들 때 이 문서와 어긋나면 이 문서가 이긴다.
> AI로 화면을 수정할 때는 반드시 이 파일을 먼저 읽게 한 뒤 작업시킨다.

---

## 0. 진단 — 왜 "AI 냄새"가 나고, 왜 정보과부하인가

현재 코드베이스에서 실제로 발견된 문제를 기준으로 한다. 아래 항목은 전부 **금지 목록**이기도 하다.

### AI 냄새의 원인 (발견된 그대로)
| # | 패턴 | 실제 예 |
|---|------|---------|
| 1 | 크림색 배경 + 세리프 제목 + 테라코타 빨강 조합 | `#FAF7F2` + Hahmlet + `#C0392B` — AI 생성 디자인의 가장 흔한 기본값 |
| 2 | UI 텍스트 속 이모지 | `🔮 ⚠️ 🧹 ✦ 🔒 💧 🔥 ⚔️ ⛰️` |
| 3 | 깜빡이는 점이 달린 알약(pill) 배지 | `천문 만세력 × 오행 케미스트리` + `animate-pulse` 점 |
| 4 | 제목 위에 얹는 "eyebrow" 라벨 남발 | 모든 카드가 배지 → 아이콘 라벨 → 제목 → 설명 4층 구조 |
| 5 | 순서가 아닌 것에 붙인 01/02/03/04 번호 카드 | "4대 핵심 분석 영역" 벤토 그리드 |
| 6 | 그라디언트 배경·버튼 | `from-[#2C3E50] to-[#1E293B]`, `from-amber-600 to-red-600` |
| 7 | 깜빡이는(pulse) CTA | "정회원 승급" 버튼 `animate-pulse` |
| 8 | 한 화면에 장식용 색 5종 이상 | amber, emerald, blue, red, slate 뱃지가 동시 등장 |
| 9 | 9~11px 볼드 마이크로 타이포 벽 | `text-[11px] font-serif font-bold`가 화면 전체를 뒤덮음 |
| 10 | 과장 마케팅 카피 | "단 1초의 오차도 없이 완벽하게", "최고급 성학", "정밀"의 반복 |
| 11 | 지어낸 수치·후기 | 근거 없는 "만족도 98%", "10만 명이 선택" 류 — 실제 수치가 없으면 쓰지 않는다 |
| 12 | 아이콘+제목+설명 3단 등폭 그리드 | 모든 LLM이 뱉는 기본형. 쓰려면 폭·높이를 다르게 깨거나 타이포 리듬으로 대체 |
| 13 | 이탤릭 제목, 그라디언트 글자 | 제목은 항상 정체(roman)·단색 먹. 강조는 굵기나 인주 한 점으로 |
| 14 | 가짜 브라우저/폰 프레임 | 신호등 점 3개 달린 목업 크롬 재작도 금지. 실제 스크린샷이나 콘텐츠 그대로 |
| 15 | 100vh 중앙정렬 히어로 | 히어로는 내용만큼의 높이. 문장 하나+버튼 하나 가운데 배치는 기본형 |

### 정보과부하의 원인
첫 방문자가 랜딩 한 화면에서 마주치는 것: 탭 스위처 + 배지 + 헤드라인 + 분석엔진 스펙 카드 + 액션 카드 2개 + 4대 기능 그리드 + 학술 칼럼 4편 + FAQ 4개 + 떠 있는 왕관 상점 버튼. **처음 온 사람이 해야 할 일은 단 하나(내 사주 보기 또는 코드 입장)인데, 화면은 열 가지를 동시에 말하고 있다.**

---

## 1. 디자인 컨셉 — "종이 위의 먹과 인주(印朱)"

화면은 흰 종이처럼 조용하고, 붉은색은 도장과 핵심 행동에만 찍힌다.

- **종이**: 채도 없는 근백색. 노란 크림색 금지.
- **먹**: 텍스트는 먹색 단일 계열 3단계만.
- **인주**: 빨강은 (1) 낙관 마크, (2) 화면당 1개의 주 행동 버튼, (3) 활성 상태 — 이 3곳 외 사용 금지.
- **오행색**: 데이터(차트·오행 칩)에만. 장식 금지.

### 시그니처: 수묵 작품 + 낙관
이 서비스의 얼굴은 **먹 한 가지 색으로 그린 수묵 산수 일러스트에 인주 한 점**이 찍힌 장면이다.
메인 게이트웨이의 진입 카드처럼 "작품"이 필요한 자리에는 디자인 리소스를 아끼지 않고 제대로 그린다. 단, 규칙이 있다:

- **먹 단색**: 일러스트는 먹(ink) 계열 농담(濃淡)만으로 그린다. 금색 구름·오색 장식 금지.
- **인주 한 점**: 장면당 붉은 요소는 딱 하나(해, 학의 단정丹頂, 낙관 등). 그 한 점이 시선을 모은다.
- **원근은 농도로**: 먼 산일수록 옅게(불투명도 0.1대), 가까울수록 진하게 — 실제 수묵화의 삼원법을 따른다.
- **작품에는 낙관을 찍는다**: 수묵 카드의 모서리 낙관은 "낙관 화면당 1개" 규칙의 예외다 (그림의 서명이므로).
- **텍스트는 그림 밖에서**: 일러스트 위에 글자를 얹지 않는다. 제목과 한 문장은 그림 아래 종이 영역에.
- 수묵 일러스트는 **화면당 최대 2점**, 게이트웨이·결과 카드처럼 상품 가치가 걸린 자리에만. 일반 화면은 여전히 조용하게.

---

## 2. 컬러 토큰

`src/index.css`의 `@theme`에 정의된 토큰만 사용한다. **임의 hex 직접 입력 금지.**

| 토큰 | 값 | 용도 |
|------|-----|------|
| `--color-paper` | `#FCFCFA` | 페이지 배경 |
| `--color-surface` | `#FFFFFF` | 카드·모달 배경 |
| `--color-sunken` | `#F4F4F1` | 입력창 배경, 눌린 영역 |
| `--color-line` | `#E7E7E2` | 모든 테두리·구분선 |
| `--color-ink` | `#1C1D21` | 제목, 본문 강조 |
| `--color-ink-soft` | `#55565E` | 본문 |
| `--color-ink-faint` | `#8E8F98` | 캡션, 비활성 |
| `--color-seal` | `#B3382C` | 낙관, 주 CTA, 활성 상태 (화면당 최소 사용) |
| `--color-seal-deep` | `#98291F` | seal hover |
| `--color-wood` | `#3E7C4F` | 오행 木 (데이터 전용) |
| `--color-fire` | `#C24234` | 오행 火 (데이터 전용) |
| `--color-earth` | `#B07C3F` | 오행 土 (데이터 전용) |
| `--color-metal` | `#7D848E` | 오행 金 (데이터 전용) |
| `--color-water` | `#35597A` | 오행 水 (데이터 전용) |

금지: amber/emerald/blue/red 등 Tailwind 기본 팔레트의 장식적 사용, 모든 그라디언트, 배경색이 다른 뱃지 3종 이상 동시 사용.

## 3. 타이포그래피

| 역할 | 클래스 | 크기 | 폰트 |
|------|--------|------|------|
| 화면 제목 (h1) | `font-serif text-2xl sm:text-3xl font-semibold` | 24→30px | Hahmlet |
| 섹션 제목 (h2) | `font-serif text-lg font-semibold` | 18px | Hahmlet |
| 카드 제목 (h3) | `text-[15px] font-semibold` | 15px | Noto Sans KR |
| 본문 | `text-sm leading-relaxed` | 14px | Noto Sans KR |
| 캡션 | `text-xs` | 12px | Noto Sans KR |

**규칙**
- Hahmlet(세리프)은 h1·h2·낙관 글자에만. 버튼·라벨·캡션에 `font-serif` 금지.
- 12px 미만 텍스트 금지 (현재 9~11px 남발 → 전부 12px 이상으로).
- 11px 이하 + `font-bold` 조합 절대 금지.
- 본문 줄간격 `leading-relaxed`(1.625) 이상, `word-break: keep-all` 유지.

## 4. 레이아웃·컴포넌트 규격

- **컨테이너**: 모바일 우선 단일 컬럼. 콘텐츠 페이지 `max-w-xl`, 리포트 페이지 `max-w-2xl`. 6xl 금지.
- **페이지 프레임에 보더 금지**: 페이지 전체를 감싸는 흰 카드 프레임을 만들지 않는다. 콘텐츠는 종이(paper) 배경 위에 바로 놓고, 헤더/푸터는 가로줄(border-b/t) 하나로만 구분한다.
- **모서리**: 카드·버튼·입력창 모두 `rounded-xl`(12px) 하나로 통일. 3xl/full 혼용 금지 (알약 배지 금지와 연동).
- **그림자**: 모달·드롭다운 등 떠 있는 표면에만 `shadow-lg`(이때 보더는 생략). 카드는 그림자 없이 `border border-line`.
- **여백**: 섹션 간 `space-y-10` 이상. 좁은 여백에 콘텐츠를 눌러 담지 않는다.

### 보더는 한 겹만 (중첩 금지)
**화면에서 테두리를 가질 수 있는 것은 종이 위에 바로 놓인 카드 한 겹뿐이다.**
보더 안에 또 보더가 들어가는 순간(카드 안의 테두리 박스, 그 안의 테두리 입력창…) 화면이 액자 속 액자가 된다.

- 카드 내부 요소는 전부 **보더 없이** 구분한다: 배경색(`bg-sunken`), 여백, `divide-y divide-line`(리스트 행 구분선) 만 사용.
- 카드 안의 입력창: `bg-sunken` 무보더, 포커스 시에만 `ring-1 ring-ink`.
- 카드 안의 버튼(secondary): `bg-sunken hover:bg-line` 무보더.
- 카드 안의 배지·칩: `bg-sunken` 무보더.
- 모달·드롭다운: `shadow-lg` 무보더 (떠 있는 표면은 그림자가 경계 역할).
- 예외 없음. "구분이 약해 보여서" 보더를 추가하고 싶다면 여백을 늘리는 것이 정답이다.

**버튼** (화면당 primary는 1개)
```
primary   : bg-seal hover:bg-seal-deep text-white text-sm font-semibold rounded-xl px-5 py-3
secondary : bg-sunken hover:bg-line text-ink text-sm font-semibold rounded-xl px-5 py-3 (무보더)
ghost     : text-ink-soft hover:text-ink text-sm px-3 py-2
```

**카드** (종이 위 1겹만 허용)
```
bg-surface border border-line rounded-xl p-5 (그림자·그라디언트·배경 SVG 금지, 내부에 보더 요소 금지)
```

**수묵 카드** (게이트웨이 등 시그니처 자리 전용, §1 규칙 준수)
```
border border-line rounded-xl overflow-hidden — 상단: 수묵 SVG 장면(먹 단색 + 인주 1점 + 모서리 낙관),
하단: bg-surface 텍스트 영역(제목 + 한 문장). 그림 위 텍스트 금지.
```

**입력창**
```
bg-sunken rounded-xl px-4 py-3 text-sm (무보더)
focus: outline-none ring-1 ring-ink (빨간 포커스 링 금지 — 빨강은 인주 전용)
```

**낙관 (시그니처)**
```
bg-seal text-white font-serif rounded-md — 한자 1글자 (緣, 命 등)
헤더 로고 8x8(32px), 화면 내 앵커 12x12(48px). 화면당 1개.
```

## 5. 문구(카피) 규칙

1. **UI 텍스트에 이모지 금지.** 아이콘이 꼭 필요하면 lucide 아이콘 1개, 그것도 의미가 있을 때만.
2. **과장 금지.** "단 1초의 오차도 없이 완벽하게" → "절기 입기 시각까지 반영해 계산합니다". 형용사보다 사실.
3. **버튼은 정확한 동사.** "보러가기" → "내 사주 보기". 흐름 내내 같은 단어 유지 (버튼 "입장하기" → 완료 메시지도 "입장").
4. **해요체 통일.** 감탄사·느낌표 남용 금지.
5. **경고는 내용으로.** "⚠️ 탈퇴 전 반드시 확인해 주세요:" → "탈퇴하면 아래 데이터가 삭제됩니다".
6. 한자 병기는 의미를 더할 때 1회만 (예: 첫 등장 시 "사주四柱"). 모든 라벨에 한자 반복 금지.

## 6. 정보 구조(IA) 규칙 — 정보과부하 방지

1. **화면당 주 행동 1개.** 첫 화면 기준: 헤드라인 1 + 보조문장 1 + 선택지 최대 2.
2. **첫 화면(스크롤 전)에는 서비스 설명을 하지 않는다.** 뭘 할 수 있는지 선택지로 보여준다.
3. **부가 콘텐츠는 접는다.** 학술 칼럼·FAQ·법적 고지·기능 소개는 랜딩 하단 접힘(아코디언/링크)으로. 첫 화면에 펼쳐놓지 않는다.
4. **떠 있는(floating) 버튼 금지.** 상점 진입은 헤더 또는 결과 화면의 맥락 안에서.
5. **뱃지·필·카운터는 정보가 있을 때만.** 장식용 배지("Analysis Engine" 스펙 카드 등) 제거.
6. **모든 리스트 기본 3개 노출 + 더보기.** (기존 방 목록 정책 유지)

## 7. 모션

- 페이지 전환 fade 1회, 리스트 등장 stagger 정도까지만.
- `animate-pulse`·`animate-spin`(로딩 제외)·hover 시 scale 확대 금지.
- `prefers-reduced-motion` 존중.

---

## 8. 적용 단계

| Phase | 대상 | 상태 |
|-------|------|------|
| 1 | 토큰(`index.css`), `Layout`(헤더), `Footer`, `GatewayView`(첫 진입), `LandingView`(그룹 랜딩) | ✅ 완료 |
| 2 | `SajuForm`, `MySajuView` (입력~개인 소울카드) | ✅ 완료 |
| 3 | `CreateView`, `JoinView`, `RoomView`, `GroupView`, `GroupNetwork`, `MeView`, `SajuVisual`, `GoogleAds` + 모달 전체(`PremiumPaywall`, `PairChemistryModal`, `ViralCardModal`, `PdfReportModal`, `AuthModal`, `UpgradeToSocialModal`, `LegalModal`, `LoadingOverlay`, `SurveyPopup`, `KakaoOutlinkGuide`, `MbtiTest`) | ✅ 완료 |
| — | `AdminView` (관리자 전용, 사용자 미노출) | 별도 진행 |

작업 시 로직(상태, 핸들러, Firebase 호출, 분석 이벤트, id 속성)은 건드리지 않고 프레젠테이션만 바꾼다.

### 디자인 확인용 개발 화면
`#/dev-qa` (개발 빌드 전용, `import.meta.env.DEV` 가드) — 로그인이나 실데이터 없이
1:1 궁합 모달·공유 카드·감정서 리포트를 목업 멤버로 띄워 확인한다. 프로덕션 빌드에서는 라우트가 열리지 않는다.

---

## 9. Hallmark 스킬 연동

이 프로젝트에는 [Nutlope/hallmark](https://github.com/Nutlope/hallmark) 디자인 스킬이 설치되어 있다 (`.claude/skills/hallmark`).
Hallmark는 프로젝트 루트의 `design.md`(이 문서)를 **잠긴 디자인 시스템**으로 인식하고 그에 따른다 — 즉 이 문서가 항상 우선이다.

**사용법**
- 새 화면/컴포넌트 작업 전: hallmark 스킬을 로드해 이 문서의 토큰 안에서 작업한다.
- `hallmark audit <파일>` — 화면을 AI티 안티패턴 목록에 대고 채점만 (수정 없음).
- `hallmark redesign <파일>` — 기존 구현 경계 안에서 시각 구조만 재설계.

**작업 후 자가 검문 (hallmark의 6대 규율 요약)**
1. 내보내기 전 자가 채점 — 철학/위계/실행/구체성/절제/다양성 각 1~5점, 3점 미만이면 수정.
2. 지어낸 콘텐츠 금지 — 수치·후기·로고는 실제 있는 것만.
3. 토큰 밖 즉흥 금지 — 색·폰트는 반드시 §2·§3 토큰 참조. 임의 hex 즉석 삽입 금지.
4. 가짜 크롬 재작도 금지.
5. 모바일 320/375/414/768px 4폭 검증 — 가로 스크롤 없음, 버튼 두 줄 금지.
6. 제목은 항상 정체(이탤릭 금지).
